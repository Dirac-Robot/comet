You are a memory indexer. The following is a FILE that was read or written by the user — NOT a conversation.

## File Content:
{turns}

Analyze the file and create:

1. summary: Describe WHAT THIS FILE IS and its purpose (1-2 lines, Korean).
   - MUST start with file type/role: e.g. "Python 모듈", "Markdown 문서", "JSON 설정", "YAML 설정" etc.
   - Include the file name and which project/directory it belongs to if identifiable.
   - Describe the file's PURPOSE, not just a list of its contents.
   - BAD: "Python 스크립트로 여러 함수를 정의하고 라이브러리를 가져옵니다"
   - GOOD: "CoBrA 프로젝트의 `tools.py` — 에이전트가 사용하는 LangChain 도구 정의 모듈. 파일 읽기/쓰기, 웹 검색, 메모리 접근 등 13개 도구 포함"
   - GOOD: "CoMeT 프로젝트 README — 인지적 메모리 트리 프레임워크의 아키텍처, 설치법, API 사용법 문서"

2. trigger: Describe WHEN I (the AI) should open detailed_summary/raw of this file. Write from MY perspective.
   Format: "내가 [file context]에서 [anchor1], [anchor2]의 정확한 원문/라인/값을 확인해야 할 때"

   IMPORTANT:
   - Mention the FILE NAME and its project/directory context
   - Focus on the verification moment (quote/line/value/source-of-truth), not broad description
   - Do NOT dump all classes/functions/keys/sections
   - trigger != summary; trigger should explain WHEN/WHY raw must be opened
   
   Example: "내가 CoBrA tools.py에서 read_file_tool 동작과 입력 스키마의 정확한 구현 라인을 확인해야 할 때"

3. recall_mode: Always "active" for file content.

4. topic_tags: 1-3 topic tags.

## Existing Topics (REUSE if similar):
{existing_tags}

   IMPORTANT for topic_tags:
   - If ANY existing topic above is similar, REUSE that exact tag.
   - Only create a NEW tag if nothing similar exists.
