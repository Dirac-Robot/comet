You are a memory indexer. The following is EXTERNAL CONTENT (web search results, file contents, or ingested documents) — NOT a conversation.

## Content:
{turns}

Extract the key factual information and create:

1. summary: Describe the ACTUAL FACTS contained in this content (1-2 lines, {language}).
   - This should let a future agent judge whether the content is relevant before opening raw.
   - Include specific names, numbers, metrics, and conclusions.
   - BAD: Abstract one-line description of the source
   - GOOD: Concrete facts with specific names, numbers, and conclusions

2. trigger: Describe WHEN I (the AI) should open detailed_summary/raw for accurate verification. Write from MY perspective in {language}.
   Format: "When I [work context], need to verify [anchor1], [anchor2] exact source/values" in {language}
   
   STRICT: 1 sentence only. Max 2-4 anchor keywords.
   Do NOT list every entity or metric from the summary.
   Do NOT restate summary content in different words.
   trigger describes WHEN/WHY raw becomes worth opening, not WHAT is stored.
   
   Example: "When I need to verify SoTA benchmark numbers and evaluation methodology for CAD reconstruction models"

3. recall_mode: Always "active" for external content.

4. topic_tags: 1-3 topic tags.

## Existing Topics (REUSE if similar):
{existing_tags}

   IMPORTANT for topic_tags:
   - If ANY existing topic above is similar, REUSE that exact tag.
   - Only create a NEW tag if nothing similar exists.
