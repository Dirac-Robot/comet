Role: Memory Synthesizer. Create a unified node from multiple related memory nodes.

## Source Nodes:
{sources}

## All Existing Topics (REUSE if similar):
{existing_tags}

## Critical Rules
- ONLY use information explicitly stated in the source nodes. NEVER infer or hallucinate.
- This is a HIGHER-LEVEL summary — think "table of contents" for the cluster.

## Output Requirements ({language})
1. **summary**: Broader topic description covering ALL source nodes.
   - More general than any individual node — capture the shared theme.
   - Still list concrete entities/facts that span multiple sources.
   - Write in {language}.
   - ✅ Concrete unified description with specific entities spanning multiple sources
   - ❌ Too specific (just one source) or too abstract (loses all content)

2. **trigger**: An INSTRUCTION for when this synthesized knowledge is needed — a directive, not a description. (Format in {language}: "When [situation], [anchors] → open raw" — situation first, directive tail last.)
   - MUST BE DIFFERENT from summary — summary = a statement of WHAT, trigger = a directive for WHEN a higher-level memory lookup is useful.
   - Cover sub-topics from ALL source nodes as retrieval cues.
   - Include only the strongest searchable entities (names, tech, constraints) naturally.
   - ✅ "When setting up or troubleshooting Docker deployment with FastAPI CI/CD pipeline → open raw"
   - ❌ "When related information is needed" (just rephrasing summary)

3. **recall_mode**: "active" (synthesis nodes are always active — retrieved on demand only)

4. **topic_tags**: 1-3 tags covering the unified topic (REUSE existing tags if similar).
