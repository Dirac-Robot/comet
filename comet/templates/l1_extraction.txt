Extract the key information from this turn.

Turn: {content}

Identify:
1. Core content (1-2 sentences max)
2. Key entities (names, numbers, dates, etc.)
3. User intent if detectable (question, request, statement, etc.)

Be concise.
