Two memory nodes have been merged. Generate a new summary and trigger for the combined content.

## Keeper Node
Summary: {keeper_summary}
Trigger: {keeper_trigger}

## Absorbed Node
Summary: {absorbed_summary}
Trigger: {absorbed_trigger}

## Rules ({language} output)
1. **summary**: Merge BOTH summaries into a single factual index.
   - Include ALL concrete facts, decisions, entities from both nodes.
   - If topics overlap, deduplicate naturally. If they differ, separate with "; "
   - Write in {language}.
   - ✅ Concrete merged facts, semicolon-separated if multiple topics
   - ❌ Abstract one-line label that loses content

2. **trigger**: An INSTRUCTION telling the future agent when to open detailed_summary/raw of this combined node — a directive, not a description. (Format in {language}: "When [situation], [anchors] → open raw" — situation first, directive tail last.)
   - MUST BE DIFFERENT from summary — summary = a statement of WHAT is in the node, trigger = a directive for WHEN to open raw.
   - 1 sentence only. Max 2-4 anchors.
   - Do NOT dump every entity from both nodes.
   - Do NOT restate summary content in different words.
   - Combine retrieval scenarios from both nodes naturally.
   - ✅ "When the exact wording of exclusion rules and budget limits must be verified before recommending → open raw"
   - ❌ "When related information is needed" (too vague — a directive that fires constantly is noise)
   - ❌ Summary content rewritten as a long trigger
